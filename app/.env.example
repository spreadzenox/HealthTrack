# App version / build ID. Set at build time so the in-app update banner
# (GitHub Releases) compares the correct version.
# Example: git commit SHA, CI run number, or semver (1.0.1)
# VITE_APP_VERSION=1.0.0
